---
import type { ImageMetadata } from "astro";
import { Image } from "astro:assets";

interface Props {
	src: string;
	alt: string;
	name: string;
	age: number;
}

const { src, alt, name, age } = Astro.props;
let url = new URL(src, "http://blog.shrirambalaji.com");
---

